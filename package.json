{
  "name": "create-booster-labels",
  "version": "1.0.0",
  "description": "Create labels",
  "repository": ".",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "parse": "babel-node parse.js data.csv > data.json",
    "generate": "npm run parse && mustache data.json template.mustache > data.html",
    "summarize": "npm run parse && babel-node summarize.js data.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "csv": "^1.1.1",
    "mustache": "^2.3.0"
  }
}
